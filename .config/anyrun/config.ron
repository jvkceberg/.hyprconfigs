Config(
  x: Fraction(0.5),
  y: Fraction(0.3),

  width: Fraction(0.3),
  height: Fraction(0.0),

  hide_at_start: false,
  close_on_click: true,

  plugins: [
    "libapplications.so",
    "libsymbols.so",
    "libshell.so",
    "libtranslate.so",
  ],
)
